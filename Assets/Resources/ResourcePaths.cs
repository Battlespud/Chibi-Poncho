using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ResourcePaths : MonoBehaviour {

    //will contain paths through the resources directory for loading assets at runtime through scripts
    //variables should be public static readonly
}
